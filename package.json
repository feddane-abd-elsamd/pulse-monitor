{
  "name": "pulse-monitor",
  "version": "0.1.0",
  "description": "Browser-based SaaS-style system monitor prototype",
  "private": true,
  "scripts": {
    "dev": "npx serve -l 8000 .",
    "start": "npx serve -l 8000 .",
    "deploy": "wrangler pages deploy .",
    "deploy:preview": "wrangler pages dev ."
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.160",
    "@composio/core": "^0.10.0"
  },
  "devDependencies": {
    "serve": "^14.2.4",
    "wrangler": "^3.90.0"
  },
  "engines": {
    "node": ">=20"
  }
}
